tests:
  - name: runs addition
    fixture: arithmetic
    command: ["run", "{fixture_path}/add.bvm"]
    expect:
      exit_code: 0
      stdout_exact: "7\n"

  - name: halt skips later instructions
    fixture: arithmetic
    command: ["run", "{fixture_path}/halt_early.bvm"]
    expect:
      exit_code: 0
      stdout_exact: "1\n"
