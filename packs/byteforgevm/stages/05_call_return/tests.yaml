tests:
  - name: calls function and returns
    fixture: calls
    command: ["run", "{fixture_path}/call.bvm"]
    expect:
      exit_code: 0
      stdout_exact: "12\n"

  - name: call preserves guest values
    fixture: calls
    command: ["run", "{fixture_path}/preserve_value.bvm"]
    expect:
      exit_code: 0
      stdout_exact: "12\n"
