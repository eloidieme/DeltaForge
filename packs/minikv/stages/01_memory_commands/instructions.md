# In-memory commands

Implement:

```bash
minikv memory <key> <value>
```

The command should store the key/value pair in memory and print:

```txt
key=value
```

Non-goals: persistence, deletes, compaction.
